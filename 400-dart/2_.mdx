---
title: "Değişkenler"
layout: "okuyarakogren-content"
topic: "Değişkenler"
description: "Bu derste Dart hakkında genel bir bilgilendirme yapılmaktadır."
slug: dart/2_
prev_page: /dart/1_
prev_title: "İndirme ve Kurma"
next_page: /dart/3_
next_title: "Operatörler"
git_page_link: https://github.com/buraxta/okuyarakogren-content/blob/master/400-dart/2_.mdx
pageType: "dart"
---

<Box>
<BoxTitle>Selam Kod Kaşifi! 👋</BoxTitle>
<ParagraphHighlight>
Şimdi kodlama dünyasının temel taşlarından birine adım atıyoruz: \
Değişkenler (Variables)!
Değişkenler, programların hafızası gibidir; bilgileri saklar, taşır ve gerektiğinde senin için işleme alır.

Bir sihirli kutu düşün: İçine bir sayı, bir isim ya da bir kelime koyabilirsin. İşte değişkenler tam da böyle çalışır! Kodunun enerjisi ve zekası buradan gelir. Hazır mısın? Değişkenlerin büyülü dünyasına dalıyoruz! 🚀

</ParagraphHighlight>
</Box>

## **1. Değişken Tanımlama ve Başlatma**

```ts
var name = "Okuyarak Öğren";
```

- Bu satırda:\
  `var` anahtar kelimesi ile bir değişken tanımlanır.
  `name` adlı değişken, "Okuyarak Öğren" adlı bir String objesine referans verir.

- Eğer değişken türünü açıkça belirtmek isterseniz, `String` gibi bir tür kullanabilirsiniz:

```ts
String name = 'Okuyarak Öğren';
```

## **2. Type Inference (Tür Çıkarımı)**

- Dart, değişken türlerini otomatik olarak anlayabilir. Bu nedenle var kullanıldığında, Dart türü otomatik olarak belirler. Ancak türü açıkça belirtmek isterseniz, ilgili türü yazabilirsiniz.

```ts
Object name = 5 ;                // name herhangi bir türde değer alabilir.
String name = 'Okuyarak Öğren';  // name sadece String türünde değer alabilir.
```

## **3. Null Safety (Boş Değere Karşı Güvenlik)**

- Dart, `null` güvenliği ile çalışır. Bu, bir değişkenin boş (null) değere sahip olmasından kaynaklanan hataları önler.`

### Değişken Türleri ve Null Güvenliği:

`String?` gibi bir tür, boş değeri (null) alabilir.
`String` gibi bir tür ise boş olamaz, yani bir başlangıç değeri atanmalıdır.

```ts
String? name;           // Boş olabilir
String surname = 'Doe'; // Boş olamaz
```

### Zorunlu Başlangıç Değeri:

- Boş olamayacak bir değişkeni mutlaka başlatmanız gerekir:

```ts
int number = 13; // Başlangıç değeri zorunlu
```

## **4. Late Anahtar Kelimesi**

- `late`, bir değişkenin başlatılmasını daha sonra yapacağınızı belirtir. Dart derleyicisi bu durumu kabul eder.

```ts
late String description;
description = 'Dart is fun!';
```

<Alert type="warning">
  Ancak, `late` değişkeni kullanmadan önce başlatmanız gerekir; aksi halde çalışma zamanında hata
  alırsınız.
</Alert>

## **5. Final ve Const Değişkenler**

- Eğer bir değişkenin değerini değiştirmek istemiyorsanız, final veya const kullanabilirsiniz.

<Alert type="note">Örnek değişkenler final olabilir ancak const olamaz.</Alert>

- Final: Değişken sadece bir kez atanabilir. Değeri çalışma zamanında belirlenir.

```ts
final name = 'Okuyarak Öğren';
```

- Const: Değeri derleme zamanında sabitlenir ve değiştirilemez.

```ts
const pi = 3.14;
```

### final vs const

-> **final kullanılacak durumlar**

1. Çalışma Zamanında Belirlenen Değerler:

- Değerin çalışma zamanında belirlendiği ancak sonrasında değiştirilmeyeceği durumlarda final kullanılır.\
  Örneğin, bir API'den alınan veriler veya kullanıcıdan alınan girişler.

2. Değerler Dinamik Olarak Atanabilir:
   Eğer değer çalışma zamanında bir işlem veya hesaplama sonucunda belirlenecekse, final uygun bir tercihtir.

-> **const Kullanılacak Durumlar**

1. Derleme Zamanında Sabitler:

- Derleme zamanı sabitleri (örn. matematiksel sabitler veya yapılandırma değerleri) için const kullanılır.\
  Bu, değişkenin her yerde aynı kalmasını garanti eder.

2. Sabit Koleksiyonlar:
   Koleksiyonlar (listeler, haritalar) içindeki tüm öğeler derleme zamanında sabitse, const ile tanımlanabilirler.\
   Bu, koleksiyonun içeriğinin değişmeyeceğini garanti eder.

## Quiz

<TabbedContent>
<TabItem value="Soru"> 
<br/>
Dart dilinde bir değişkeni null değer alabilir şekilde nasıl tanımlarsınız?
<br/>
</TabItem>

<TabItem value="Cevap"> 
<br/>
`String? name;` şeklinde değişken türünden sonra `?` işareti koyarak tanımlarız.
<br/>
</TabItem> 
</TabbedContent>

<TabbedContent code >
<TabItem value="Soru"> 
<br/>
final ve const arasındaki temel fark nedir?
<br/>
</TabItem>

<TabItem value="Cevap"> 
<br/>
final değişkenler çalışma zamanında (runtime) bir kez değer alabilirken, const değişkenler derleme zamanında (compile time) değer alır ve değerleri sabittir.
<br/>
</TabItem> 
</TabbedContent>

<TabbedContent code >
<TabItem value="Soru"> 
<br/>
Dart'ta tür belirtmeden bir değişken nasıl tanımlanır ve buna ne ad verilir?
<br/>
</TabItem>

<TabItem value="Cevap"> 
<br/>
 var anahtar kelimesi kullanılarak tanımlanır (örn: var name = 'Okuyarak Öğren';) ve buna Type Inference (Tür Çıkarımı) denir.
<br/>
</TabItem> 
</TabbedContent> 
<TabbedContent>
<TabItem value="Soru"> 
<br/>
late anahtar kelimesinin kullanım amacı nedir?
<br/>
</TabItem>

<TabItem value="Cevap"> 
<br/>
late, bir değişkenin değerinin daha sonra atanacağını belirtir. Ancak değişken kullanılmadan önce mutlaka değer atanmalıdır, aksi halde çalışma zamanında hata alınır.
<br/>
</TabItem> 
</TabbedContent>

<TabbedContent>
<TabItem value="Soru"> 
<br/>
Aşağıdaki kod neden hatalıdır?
```Dart
String name;
print(name);
```
<br/>
</TabItem>

<TabItem value="Cevap"> 
<br/>
Çünkü Dart'ın null safety özelliği gereği, String türündeki değişkenler (? olmadan tanımlandığında) null değer alamaz ve mutlaka başlangıç değeri atanmalıdır.
<br/>
</TabItem> 
</TabbedContent>
